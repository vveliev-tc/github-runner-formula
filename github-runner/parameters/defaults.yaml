# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: github-runner
  rootgroup: root
  config: '/etc/github-runner'
  service:
    name: github-runner
  subcomponent:
    config: '/etc/github-runner-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
